# Legacy chapters

The pre-refactor pipeline, kept for reference only. Not part of the reproduction
path.

Relative data paths inside these files no longer resolve (they moved one
directory deeper), and the R scripts no longer sit next to `renv` (now
`chapters/r-env`, see `tools/run_r.sh`).

Where each part went is in `REFACTOR_PLAN.md`; what was dropped is in `GAPS.md`
§4.
